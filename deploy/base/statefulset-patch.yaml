apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: statefulset
  labels:
    tier: mariadb
spec:
  serviceName: "mariadb"
  selector:
    matchLabels:
      tier: mariadb
  template:
    metadata:
      labels:
        tier: mariadb
    spec:
      serviceAccountName: scheduler-api-serviceaccount
      containers:
        - name: mariadb
          env:
            - name: MARIADB_ROOT_HOST
              value: scheduler-api-mariadb-service
