apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: public-endpoint
spec:
  tls:
    - hosts:
        - bsn-openlab-scheduler.ltc.bcit.ca
      secretName: star-ltc-bcit-ca
  rules:
    - host: bsn-openlab-scheduler.ltc.bcit.ca
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: service
                port:
                  number: 80
